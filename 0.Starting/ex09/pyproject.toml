[project]
name = "ft_package_janeway"
version = "0.0.1"
authors = [
	{ name="janeway", email="janeway@example.example" },
]
description = "A small example package"

[build-system]
requires = ["setuptools >= 77.0.3"]
build-backend = "setuptools.build_meta"